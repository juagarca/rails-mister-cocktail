<div class="container" style="width: 500px;">
  <% if @cocktail.photo.attached? %>
    <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path @cocktail.photo.key, height: 300, width: 300, crop: :fill %>')"><h1><%= @cocktail.name %></h1></div>
  <% end %>
  <br>
  <div class="container text-center" style="width: 350px;">
  <p>Ingredients and measurements:</p>

    <% @cocktail.doses.each do |d| %>
      <p>- <%= d.description %> | <%= d.ingredient.name %> |
        <%= link_to 'ⓧ', dose_path(d.id),
              method: :delete,
              data: { confirm: "Are you sure?" } %>
      </p>
    <% end %>
  <br>
  <%= link_to 'New dose', new_cocktail_dose_path(@cocktail.id), class: "button" %> <br> <br>
  <%= link_to '🔙 to cocktails', cocktails_path, class: "button" %>
  </div>
</div>

