<h1>Cocktail <%= @cocktail.name %> </h1>

<div class="ingredients">
  <ul>
    <% @cocktail.doses.each do |d| %>
      <li><%= d.description %> | <%= d.ingredient.name %> |
        <%= link_to '🗑', dose_path(d.id),
              method: :delete,
              data: { confirm: "Are you sure?" } %>
      </li>
    <% end %>
  </ul>
</div>

<%= link_to 'Create a new dose', new_cocktail_dose_path(@cocktail.id) %>
